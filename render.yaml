services:
  - type: web
    name: chargeguard-api
    runtime: docker
    repo: https://github.com/amr2883/chargeguard-api.git
    branch: main
    dockerContext: .
    dockerfilePath: ./Dockerfile
    dockerCommand: "node src/app.js"
    envVars:
      - key: DATABASE_URL
        sync: false